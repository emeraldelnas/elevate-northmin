import { VidLazyLoadDirective } from './vid-lazy-load.directive';

describe('VidLazyLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new VidLazyLoadDirective();
    expect(directive).toBeTruthy();
  });
});
